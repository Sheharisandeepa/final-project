
/*var swiper = new Swiper(".hero-slider", {
    loop:true,
    effect: "coverflow",
    spaceBetween: 30,
    grabCursor: true,
    coverflowEffect: {
       rotate: 50,
       stretch: 0,
       depth: 100,
       modifier: 1,
       slideShadows: false,
    },
    navigation: {
      nextEl: ".swiper-button-next",
      prevEl: ".swiper-button-prev",
    },
 });*/

 
  /* var slideimg = document.getElementById("slideimg");
   var images = new Array(
      "img/home1.jpg",
      "img/home2.jpg",
      "img/home3.jpg"
   );
var len = images.length;
var i = 0;

function slider() {
   if(i > len-1){
      i = 0;
   } 
   slideimg.src = images[i];
   i++;
   setTimeout('slider()',3000);
}*/


// Home page Image Animation
/*alert('Hi baby...');
function handleIntersection(entries, observer) {
   entries.forEach(entry => {
     if (entry.isIntersecting) {
       // Add a class to trigger the animation when the image is in view
       document.getElementById('animatedImage').classList.add('animate');
       // Disconnect the observer since we only need to animate the image once
       observer.disconnect();
     }
   });
 }

 // Create an intersection observer
 const observer = new IntersectionObserver(handleIntersection, { threshold: 0.2 });

 // Target the image to observe
 const target = document.getElementById('animatedImage');

 // Start observing the target
 observer.observe(target);*/


 
 